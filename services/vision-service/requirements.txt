# ── API ───────────────────────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.13.0
python-multipart==0.0.20

# ── Vision / detección (modelo propio rostros.pt) ─────────────────
ultralytics==8.4.141
torch==2.14.0
torchvision==0.29.0

# ── Reconocimiento facial (ArcFace vía ONNX) ──────────────────────
insightface==1.0.1
onnxruntime==1.29.0

# ── Procesamiento de imagen ───────────────────────────────────────
numpy==2.5.2
opencv-python-headless==5.0.0.93
pillow==12.3.0

# ── Observabilidad ────────────────────────────────────────────────
structlog==25.5.0

# OpenTelemetry. La instrumentacion de FastAPI extrae por si sola la
# cabecera traceparent que envia el Face Service, y por eso el tramo de
# Python aparece dentro de la misma traza y no como una suelta.
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
